{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 90%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        td, th {
            border: 1px solid grey;
        }
        /* font-family: 'Roboto Light',system-ui" */
    </style>

    <div class="example-wrapper">
        <h1>Garage3D 🛠🧰🖨</h1>
    </div>
    <div>
        {% if user is null %}
            <br/>
            <h1>User not found</h1>
        {% else %}
            <h2>{{ user.username }}</h2>
            <br/>
            <p>{{ user.about }}</p>
            <!--{/% if user.id == app.user.id %}
                <p>Change description about yourself</p>
                sdsdfd
            {/% endif %}-->
            <br/>
            Items created by {{ user.username }}:
            <ul>
                {% for item in user.items %}
                    <li>
                        <a href="{{ path('item_page', { item_id: item.id }) }}" class="no-color-link">{{ item.itemName }}</a>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
        <br/>
        <a href="{{ path('app_main') }}">back to title page</a>
        <br/>
    </div>
    <p></p>
{% endblock %}